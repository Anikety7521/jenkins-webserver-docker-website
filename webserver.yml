- hosts: all
  tasks:
   - name: installing web server
     package:
       name: "docker"
       state: present

   - name: startig the web server
     service:
             name: "docker"
             state: started
             enabled: yes
             
   - name: ensure a container is running
     docker_container:
       name: webapp
       state: started
       image: "aniket86/webserver"
       pull: true
       ports:
         - "5000:80"
   

